# 📖 HƯỚNG DẪN SỬ DỤNG HỆ THỐNG ANTIGRAVITY (V4.2)

Hệ thống Antigravity của anh giờ đây là một binh đoàn Agent chuyên nghiệp, bảo mật và cực kỳ khắt khe. Dưới đây là cách sử dụng tối ưu nhất.

---

## 1. 🚀 CÀY ĐẶT TƯ DUY (MÔ HÌNH THÔNG MINH NHẤT)

Để đạt hiệu quả tối đa, anh nên sử dụng các câu lệnh theo trình tự "Tư duy lớp":

### 🔹 Cách gọi "Thông minh" (Smart Sequence)
1.  **`/plan [yêu cầu]`**: Luôn bắt đầu bằng việc lập kế hoạch. Điều này buộc em phải suy nghĩ về kiến trúc trước khi viết một dòng code nào.
2.  **`/visualize`**: Nếu là dự án có giao diện, hãy chạy lệnh này sau khi có plan để chốt "vẻ ngoài" Luxury.
3.  **`/code`**: Thực thi viết mã nguồn dựa trên Plan và UI đã duyệt.
4.  **`/audit`**: Gọi Vị Giám Sát khắt khe quét lại toàn bộ thành quả.
5.  **`/save-brain`**: Kết thúc buổi làm việc để lưu lại toàn bộ ngữ cảnh vào bộ nhớ dài hạn.

---

## 2. ⌨️ BẢN ĐỒ CÁC LỆNH SLASH (`/`)

| Lệnh | Ý nghĩa | Khi nào dùng? |
| :--- | :--- | :--- |
| `/start` | **Force Read Protocol** | Khi em bắt đầu "quên bài" hoặc làm sai quy tắc. |
| `/init` | Khởi tạo dự án | Khi bắt đầu một thư mục/ý tưởng mới hoàn toàn. |
| `/architect` | Thiết kế hệ thống | Khi cần xây dựng cấu trúc Multi-Agent hoặc Database phức tạp. |
| `/audit` | Giám sát khắt khe | Khi muốn kiểm tra bảo mật, lỗi logic hoặc AI Slop. |
| `/git` | Push code an toàn | Khi muốn đẩy code lên GitHub dùng Multi-Account (Bảo mật Token). |
| `/reverse` | Đảo ngược hệ thống | Khi muốn em phân tích một codebase lạ hoặc Agent khác. |
| `/theme` | Thay đổi phong cách | Khi muốn đổi "áo" cho các báo cáo hoặc giao diện. |

---

## 3. 🛡️ QUY TRÌNH "VỊ GIÁM SÁT KHẮT KHE" (AUDITOR AGENT)

Đây là tính năng mới nhất anh đã yêu cầu. Nó hoạt động như một "Gatekeeper" (Người gác cổng):
*   **Cách gọi tối ưu**: Sau khi em trả về một kết quả code/UI, anh chỉ cần gõ số tương ứng trong Menu (thường là số **4**) hoặc gõ `/audit`.
*   **Tiêu chuẩn Zero-Tolerance**: Nó sẽ bác bỏ ngay nếu thấy:
    - Code không có Header Dạ Hành Studio.
    - File dài quá 900 dòng (Monolith).
    - Có lỗ hổng bảo mật (Hardcode Secret).
    - UI dùng Emoji rẻ tiền (AI Slop).

---

## 4. 💡 MẸO GỌI AGENT TỐI ƯU (PRO TIPS)

*   **Đừng ra lệnh quá ngắn**: Thay vì gõ *"Viết web bán hàng"*, hãy gõ: *"/plan viết web bán hàng phong cách Glassmorphism, tập trung vào bảo mật và chia module nhỏ"*.
*   **Sử dụng Menu Số**: Đừng tốn công gõ lệnh dài. Em luôn hiện các số (1, 2, 3...) ở cuối mỗi câu. Anh chỉ cần gõ số đó để kích hoạt bước tiếp theo nhanh nhất.
*   **Sửa lỗi bằng cách mô tả triệu chứng**: Nếu có lỗi, đừng chỉ nói "Sửa đi", hãy gõ `/debug [lỗi]` để em tự động truy vết.

---

## 📦 ĐỊNH VỊ FILE QUAN TRỌNG
- **RULES.md**: `~/.gemini/antigravity/knowledge/RULES.md` (Toàn bộ luật lệ).
- **Workflows**: `~/.gemini/antigravity/global_workflows/` (Toàn bộ các lệnh `/`).
- **Secrets**: `.agent/secrets.local.md` (Nơi cất giữ Token bí mật của anh).

---
**Dạ Hành Studio - Project Antigravity**
*"Nghiêm túc - Bảo mật - Đẳng cấp"*
